
int blink = 13;
int control = 4;

int up = 0;
int down = 1;
int right = 2;
int left = 3;


void setup() {
  // put your setup code here, to run once:
  pinMode(blink, OUTPUT);
  pinMode(control, INPUT);
  pinMode(up, INPUT);
  pinMode(down, INPUT);
  pinMode(left, INPUT);
  pinMode(right, INPUT);

}

void loop() {
  // put your main code here, to run repeatedly:


  /**********************************************
  *Håndtering af input fra knapper              *
  ***********************************************/
  if(control){
    
  }
  else {
    
  }

  if(up){
    
  }
  else {
    
  }

  if(down){
    
  } else {
    
  }

  if(left){
    
  }
  else {
    
  }

  if(right){
    
  } else {
    
  }
  
}
